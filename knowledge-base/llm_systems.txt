==llm_systems==
Transformer Architecture||Self-attention mechanism, multi-head attention; Positional encoding (absolute, RoPE, ALiBi); Encoder-only vs decoder-only vs encoder-decoder; Layer normalization placement, feed-forward sublayers; KV cache in autoregressive generation||E:What is self-attention?||M:Why do decoder-only models dominate modern LLMs over encoder-decoder architectures?||T:Explain how RoPE encodes positional information and why it generalizes better to longer contexts than absolute positional encoding.
Model Families (Text, Vision, Voice, Multimodal)||Text: GPT family, Claude, Llama, Mistral, Gemini, Qwen; Vision: ViT, CLIP, SAM; Voice: Whisper, Wav2Vec2; Multimodal: GPT-4o, Gemini, LLaVA, Claude (vision); Open-source vs closed/paid model trade-offs (licensing, cost, control, customizability)||E:Name two open-source LLM families and two closed-source ones.||M:What are the trade-offs of self-hosting an open-source LLM vs using a paid API?||T:Design a model-selection framework for a company choosing between open-source and proprietary multimodal models, factoring cost, latency, and data privacy.
Quantization (PEFT, LoRA)||Quantization concept (FP32 -> FP16/BF16 -> INT8/INT4); LoRA (Low-Rank Adaptation) mechanics; QLoRA (quantized base + LoRA adapters); PEFT library overview; Quantization formats: GPTQ, AWQ, GGUF; Accuracy vs memory/speed trade-offs||E:What is LoRA?||M:How does QLoRA enable fine-tuning large models on consumer GPUs?||T:Compare GPTQ vs AWQ quantization and explain the accuracy/throughput trade-offs at INT4.
Fine-Tuning Methods||Supervised Fine-Tuning (SFT); Instruction fine-tuning; RLHF (Reinforcement Learning from Human Feedback) — reward model + PPO; DPO (Direct Preference Optimization) as RLHF alternative; TRL library (SFTTrainer, PPOTrainer, DPOTrainer); Full fine-tuning vs parameter-efficient fine-tuning||E:What is the difference between pretraining and fine-tuning?||M:Walk through the RLHF pipeline: from SFT model to reward model to PPO-tuned policy.||T:Compare RLHF vs DPO — why might a team choose DPO for alignment instead of full RLHF?
General LLM Concepts||Context window limitations and extension techniques; Hallucination causes and mitigation; Temperature, top-k, top-p sampling; Tokenization trade-offs (vocabulary size, subword units); Scaling laws (model size vs data vs compute); Mixture of Experts (MoE) architecture||E:What does the 'temperature' parameter control during generation?||M:Why do LLMs hallucinate, and what mitigation strategies exist beyond RAG?||T:Explain how Mixture-of-Experts models achieve larger effective parameter counts without proportional inference cost increase.
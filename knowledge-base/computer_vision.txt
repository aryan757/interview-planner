==computer_vision==
Convolutional Neural Networks (CNNs)||Convolution, stride, padding, pooling; Receptive field; Classic architectures: LeNet, VGG, ResNet, Inception; Batch normalization in CNNs; Transfer learning with pretrained CNNs||E:What is the purpose of pooling layers in a CNN?||M:Why do residual connections (ResNet) help train very deep networks?||T:Design a CNN architecture trade-off analysis for deployment on an edge device with limited compute.
Object Detection||YOLO family (v5/v8/v11) — anchor-free vs anchor-based; Two-stage detectors: Faster R-CNN; IoU, NMS (Non-Max Suppression); mAP (mean Average Precision) evaluation; Anchor boxes vs anchor-free detection heads||E:What does IoU measure?||M:How does Non-Max Suppression work and why is it needed?||T:Your YOLO model has high mAP on validation but misses small objects in production — diagnose and propose fixes.
Image Segmentation||Semantic vs instance vs panoptic segmentation; U-Net architecture; Mask R-CNN; Segmentation evaluation: IoU, Dice coefficient||E:What is the difference between semantic and instance segmentation?||M:Why is U-Net's skip-connection design well suited to segmentation tasks?||T:Design a segmentation pipeline for medical imaging where class boundaries are ambiguous and labels are scarce.
OpenCV Pipelines||Image preprocessing (resize, normalize, color space conversion); Edge detection (Canny, Sobel); Contour detection; Video stream processing; Augmentation techniques (rotation, flip, color jitter)||E:What does cv2.Canny do?||M:How would you build a real-time video preprocessing pipeline with OpenCV for a detection model?||T:Optimize an OpenCV-based preprocessing pipeline that is bottlenecking GPU inference throughput.
Vision Transformers & Modern Architectures||ViT (Vision Transformer) basics; Patch embeddings; CNN vs Transformer trade-offs for vision tasks; CLIP and vision-language pretraining||E:What is a Vision Transformer (ViT)?||M:When would you choose a ViT over a CNN for an image classification task?||T:Explain how CLIP aligns image and text embeddings, and a limitation of its zero-shot classification.
==mlops==
Git & GitHub||Branching strategies (Gitflow, trunk-based); Merge vs rebase; Pull request workflows; Resolving merge conflicts; Git for ML artifacts (large file handling, Git LFS)||E:What is the difference between merge and rebase?||M:How would you structure a branching strategy for a team shipping ML models weekly?||T:A teammate force-pushed over shared history — walk through your recovery process.
Docker & Containerization||Dockerfile basics, layers, caching; Multi-stage builds; Containerizing ML models for serving; GPU access in containers (nvidia-docker); Image size optimization||E:What is a Docker image vs a container?||M:How would you containerize a PyTorch model for GPU inference and keep image size manageable?||T:Your Docker container works locally but fails to access the GPU in production — debug systematically.
CI/CD & GitHub Actions||CI/CD concepts for ML (testing, linting, model validation gates); GitHub Actions workflow syntax (jobs, steps, triggers); Automated model retraining/deployment pipelines; Secrets management in CI/CD||E:What is CI/CD and why does it matter for ML projects?||M:Design a GitHub Actions workflow that retrains a model on new data and runs validation before deployment.||T:Design a CI/CD pipeline with automated rollback if a newly deployed model's live performance degrades.
GCP for AI/ML||Cloud Storage buckets: creating, IAM roles (Storage Object Viewer, Storage Admin); Public access configuration and risks; Vertex AI for training/serving; Cloud Run for containerized inference; Pub/Sub for event-driven pipelines; Regions and latency/availability considerations||E:What is the difference between Storage Object Viewer and Storage Admin roles in GCP?||M:Walk through creating a GCS bucket and granting public read access — and the risks of doing so.||T:Design a GCP architecture for a real-time inference pipeline using Pub/Sub, Cloud Run, and Cloud Storage, considering region placement and cost.
AWS for AI/ML||EC2 instances (GPU instance types for ML); Lambda for lightweight inference/triggers; S3 for data/model storage; SageMaker basics; IAM roles and permission scoping; Regions and availability zones||E:What is AWS Lambda typically used for in an ML pipeline?||M:When would you use EC2 over Lambda for model inference?||T:Design a cost-optimized AWS architecture for sporadic batch inference workloads vs always-on real-time inference.
Experiment Tracking & Versioning||MLflow: experiment tracking, model registry, artifact logging; DVC: data versioning, pipeline reproducibility; Weights & Biases (W&B): run tracking, hyperparameter sweeps, visualization; Comparing MLflow vs W&B vs DVC use cases||E:What problem does DVC solve that Git alone doesn't?||M:How would you set up MLflow to track experiments and register the best model for deployment?||T:Design a reproducibility strategy combining DVC + MLflow for a team running hundreds of experiments per week.